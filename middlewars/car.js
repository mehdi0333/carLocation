export function handleCarInformation(car) {
  // delete car.id;
  // delete car.permis;
  // delete car.cardGris;
  // delete car.status;
  // delete car.est_disponible;
  // delete car.__v;
  return car;
}
export function handleCarCard(car) {
  // delete car._doc.id;
  // delete car._doc.permis;
  // delete car._doc.cardGris;
  // delete car._doc.status;
  // delete car._doc.est_disponible;
  // delete car._doc.remiseMois;
  // delete car._doc.remiseSemain;
  return car;
}
